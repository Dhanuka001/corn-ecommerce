:80 {
    encode gzip

    @api path /api*
    handle @api {
        reverse_proxy backend:8080
    }

    # Everything else goes to the frontend
    handle {
        reverse_proxy frontend:3000
    }
}
